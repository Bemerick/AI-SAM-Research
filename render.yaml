services:
  # FastAPI Backend
  - type: web
    name: sam-govwin-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r backend/requirements.txt && python backend/init_db_render.py
    startCommand: python -m uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: sam-govwin-db
          property: connectionString
      - key: SAM_API_KEY
        sync: false
      - key: GOVWIN_USERNAME
        sync: false
      - key: GOVWIN_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
    healthCheckPath: /health

  # React Frontend (Static Site)
  - type: web
    name: sam-govwin-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://sam-govwin-api.onrender.com

  # SAM Fetcher Cron Job
  - type: cron
    name: sam-govwin-fetcher
    env: python
    plan: starter
    schedule: "0 */6 * * *"  # Every 6 hours
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python -m scripts.sam_fetcher_cron
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: sam-govwin-db
          property: connectionString
      - key: SAM_API_KEY
        sync: false
      - key: GOVWIN_USERNAME
        sync: false
      - key: GOVWIN_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: BACKEND_API_URL
        value: https://sam-govwin-api.onrender.com

  # AI Analyzer Cron Job
  - type: cron
    name: sam-govwin-ai-analyzer
    env: python
    plan: starter
    schedule: "0 */12 * * *"  # Every 12 hours
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python -m scripts.ai_analyzer_cron
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: sam-govwin-db
          property: connectionString
      - key: OPENAI_API_KEY
        sync: false
      - key: BACKEND_API_URL
        value: https://sam-govwin-api.onrender.com

  # GovWin Matcher Cron Job
  - type: cron
    name: sam-govwin-govwin-matcher
    env: python
    plan: starter
    schedule: "0 */24 * * *"  # Every 24 hours
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python -m scripts.govwin_matcher_cron
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: sam-govwin-db
          property: connectionString
      - key: GOVWIN_USERNAME
        sync: false
      - key: GOVWIN_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: BACKEND_API_URL
        value: https://sam-govwin-api.onrender.com

databases:
  - name: sam-govwin-db
    databaseName: sam_govwin
    user: sam_govwin_user
    plan: free  # Free tier - 256MB RAM, 1GB storage (90 day expiry)
